---
phase: 01-architecture-ui-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/backend.rs
autonomous: true
requirements:
  - UI-01
  - CORE-03

must_haves:
  truths:
    - GTK Application interacts with the D-Bus daemon to handle privileged tasks.
    - Application no longer attempts to invoke `pkexec` for `setcap`.
  artifacts:
    - path: src/backend.rs
      provides: D-Bus client proxy and connection handling.
      contains: zbus::Connection::system()
  key_links:
    - from: src/backend.rs
      to: src/ipc.rs
      via: Creating `DaemonProxy` generated by zbus macros.
      pattern: DaemonProxy::builder
---

<objective>
Update the application's backend to communicate with the newly established privileged D-Bus daemon instead of launching commands directly via `pkexec`.

Purpose: By removing inline monolithic root commands (`pkexec`), the application safely delegates required system-level networking changes (like `setcap` for TUN or starting a proxy) to the new daemon, maintaining full GNOME HIG compliance and Wayland compatibility.
Output: Refactored `backend.rs` which fetches the daemon proxy over system bus to execute proxy operations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/backend.rs
@src/ipc.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace pkexec with zbus client calls</name>
  <files>src/backend.rs</files>
  <action>
    Modify `src/backend.rs`. Remove the inline logic that executes `pkexec setcap` for TUN mode capabilities.
    Instead, inside the `CoreBackend` or equivalent place that needs privileged state, initialize a `zbus::Connection::system().await` and create a `DaemonProxy`.
    Currently, the backend spawns `xray` or `sing-box` locally. For this phase, update the error logging or stub behavior to test the D-Bus connection. You can add a `ping()` call to the daemon to verify connectivity when the backend starts.
    *Note:* Do not fully move the proxy starting logic to the daemon yet (that is Phase 2). For Phase 1, just remove `pkexec` from the GTK UI and ensure it can connect to the daemon (e.g., using `ping()`). The UI shouldn't crash if the daemon is unavailable, but should log a warning.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>`pkexec` usages are removed from `src/backend.rs` and a D-Bus client checks daemon availability.</done>
</task>

</tasks>

<verification>
Ensure `cargo check` passes and no instances of `pkexec` strings exist in `src/backend.rs`.
</verification>

<success_criteria>
The UI code is completely free of built-in monolithic privilege escalation calls, operating purely as a user-level process capable of speaking to a D-Bus daemon.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-ui-foundation/01-architecture-ui-foundation-03-SUMMARY.md`
</output>
