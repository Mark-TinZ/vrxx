---
phase: 05-ui-cleanup-core-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/pages/settings_page.rs, src/ui/pages/settings_page.ui, src/ui/pages/proxy_page.ui, po/ru.po]
autonomous: true
requirements: [UI-01, UI-04]

must_haves:
  truths:
    - "Settings page loads without runtime errors regarding missing template children"
    - "Tooltips are visible on all specified rows in Settings and Proxy pages"
    - "Russian translations for tooltips are correctly displayed when system language is Russian"
  artifacts:
    - path: "src/ui/pages/settings_page.rs"
      provides: "Clean implementation without dead UI references"
    - path: "src/ui/pages/settings_page.ui"
      provides: "UI with tooltips"
    - path: "src/ui/pages/proxy_page.ui"
      provides: "UI with tooltips"
    - path: "po/ru.po"
      provides: "Russian translations for tooltips"
---

<objective>
Remove dead UI references in the settings page and add missing tooltips to both Settings and Proxy pages to improve UX and educational value.

Purpose: Fix technical debt and fulfill UI-04 requirement.
Output: Cleaned up settings_page.rs and enriched .ui and .po files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-cleanup-core-fixes/05-RESEARCH.md
@REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead tun_mode_row references</name>
  <files>src/ui/pages/settings_page.rs</files>
  <action>
    - Remove `pub tun_mode_row: TemplateChild<adw::SwitchRow>` from `imp::VrxxSettingsPage` struct.
    - Remove `adw::SwitchRow::static_type();` from `class_init` if no other switch rows are left (though there probably are).
    - Remove the line `imp.tun_mode_row.set_active(settings.tun_mode);` from `setup_settings` function.
    - Ensure no other references to `tun_mode_row` remain in the file.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Dead references removed and code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing tooltips to UI and Translations</name>
  <files>src/ui/pages/settings_page.ui, src/ui/pages/proxy_page.ui, po/ru.po</files>
  <action>
    - Add `tooltip-text` attribute with `translatable="yes"` to the following rows in `src/ui/pages/settings_page.ui`:
        - `language_row`, `autostart_row`, `connect_startup_row`, `notifications_row`, `streamer_mode_row`, `core_selector`, `sniffing_row`, `domain_strategy_row`, `bypass_lan_row`, `fragment_row`, `log_level_row`.
    - Add `tooltip-text` attribute with `translatable="yes"` to the following rows in `src/ui/pages/proxy_page.ui`:
        - `system_proxy_switch`, `socks_port_row`, `http_port_row`, `allow_lan_switch`.
    - Use the English strings from `05-RESEARCH.md`.
    - Update `po/ru.po` by adding `msgid` and `msgstr` entries for each new tooltip using the Russian translations from `05-RESEARCH.md`.
  </action>
  <verify>
    <automated>grep "tooltip-text" src/ui/pages/settings_page.ui | wc -l</automated>
  </verify>
  <done>Tooltips added to UI files and translations added to po/ru.po.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify UI and Tooltips</name>
  <what-built>Cleaned up Settings Page and new tooltips.</what-built>
  <how-to-verify>
    1. Run the application: `cargo run`
    2. Navigate to Settings page. Confirm it doesn't crash on load.
    3. Hover over the modified rows and verify tooltips appear in English.
    4. Navigate to Proxy page and verify tooltips there.
    5. (Optional) Run with `LANG=ru_RU.UTF-8 cargo run` and verify Russian tooltips.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check for console errors when loading settings page. Verify tooltip visibility.
</verification>

<success_criteria>
1. Application launches and settings page displays without errors.
2. Tooltips are present on all specified rows.
3. Translations work for the new tooltips.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-cleanup-core-fixes/05-01-SUMMARY.md`
</output>
