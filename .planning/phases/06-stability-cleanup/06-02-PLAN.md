---
phase: 06-stability-cleanup
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/domain/key_parser.rs]
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "Malformed URLs do not cause the application to crash (panic)"
    - "The parser returns an error Result instead of panicking"
    - "Garbage strings like 'asdf://' or empty strings are handled gracefully"
  artifacts:
    - path: "src/domain/key_parser.rs"
      provides: "Hardened parse_vpn_key and extra tests"
---

<objective>
Harden the key parser against malformed or malicious inputs to ensure total application stability during key import or manual entry.
</objective>

<execution_context>
@/home/mihail/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-stability-cleanup/06-stability-cleanup-CONTEXT.md
@src/domain/key_parser.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add robustness tests and fix vulnerabilities</name>
  <files>src/domain/key_parser.rs</files>
  <behavior>
    - parse_vpn_key("") -> Err
    - parse_vpn_key("vmess://") -> Err (empty payload)
    - parse_vpn_key("vmess://!!!") -> Err (invalid base64)
    - parse_vpn_key("vless://user@") -> Ok/Err (incomplete URL)
    - parse_vpn_key("invalid://...") -> Err
    - parse_vpn_key("ss://abc@def:ghi") -> Err (invalid port)
    - ensure no panics on URLs with missing parts.
  </behavior>
  <action>
    - Add the specified test cases to the tests module in src/domain/key_parser.rs.
    - Run the tests and fix any panics or unexpected errors.
    - Ensure all existing and new tests pass.
  </action>
  <verify>
    <automated>cargo test domain::key_parser</automated>
  </verify>
  <done>All tests for malformed inputs pass without panicking.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification</name>
  <files>src/domain/key_parser.rs</files>
  <action>
    - Final check for any remaining unwrap() or expect() calls in the production code of key_parser.rs.
    - If found, replace them with safe alternatives (unwrap_or, map_err, etc.).
  </action>
  <verify>
    <automated>grep -c "unwrap()" src/domain/key_parser.rs</automated>
  </verify>
  <done>No panics exist in the parser.</done>
</task>

</tasks>

<success_criteria>
- 100% of new malformed URL tests pass.
- Application does not crash when importing garbage keys from clipboard or manual entry.
</success_criteria>
