---
phase: 06-stability-cleanup
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/domain/xray_config.rs, src/domain/mod.rs, src/daemon/mod.rs, src/ui/pages/vpn_page.rs, src/settings.rs, src/application.rs, src/main.rs, src/domain/key_parser.rs]
autonomous: true
requirements: [CORE-01, CORE-02, UI-05]
---

<objective>
Eliminate Xray-related code and streamline the backend to support Sing-box as the only proxy core.

Purpose: Reduce codebase complexity and focus on a single, high-performance engine (Sing-box).
Output: A cleaner, Sing-box-only backend architecture.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/domain/mod.rs
@src/daemon/mod.rs
@src/ui/pages/vpn_page.rs
@src/settings.rs
@src/domain/key_parser.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Removal</name>
  <files>src/domain/xray_config.rs, src/domain/mod.rs, src/daemon/mod.rs</files>
  <action>
    - Delete `src/domain/xray_config.rs`.
    - Remove `pub mod xray_config;` from `src/domain/mod.rs`.
    - In `src/daemon/mod.rs`, remove all Xray-related logic. Specifically, in `start_proxy`, remove the mapping for "xray" and simply use "sing-box". Remove the `bin_name == "xray"` conditional logic.
    - Reference: User instruction 4.
  </action>
  <verify>
    <automated>! ls src/domain/xray_config.rs 2>/dev/null && ! grep "xray_config" src/domain/mod.rs && ! grep 'bin_name == "xray"' src/daemon/mod.rs</automated>
  </verify>
  <done>Xray configuration generator and daemon integration are removed.</done>
</task>

<task type="auto">
  <name>Task 2: UI & Settings Refactor</name>
  <files>src/ui/pages/vpn_page.rs, src/settings.rs, src/application.rs, src/main.rs</files>
  <action>
    - In `src/settings.rs`, remove `core_type` from `AppSettings` or set its default to "sing-box" and remove its usage elsewhere.
    - In `src/ui/pages/vpn_page.rs`, remove all logic that checked if `bin_name == "xray"`. Specifically, remove the traffic statistics logic for Xray gRPC.
    - In `src/application.rs`, update the app description to remove references to "Xray-core" and focus on Sing-box.
  </action>
  <verify>
    <automated>! grep "xray_config::build_xray_config" src/ui/pages/vpn_page.rs && ! grep "Xray API" src/ui/pages/vpn_page.rs</automated>
  </verify>
  <done>UI and settings are refactored to remove Xray support.</done>
</task>

<task type="auto">
  <name>Task 3: Import Enhancements</name>
  <files>src/domain/key_parser.rs, src/ui/pages/vpn_page.rs</files>
  <action>
    - Refactor `parse_vpn_key` in `src/domain/key_parser.rs` to handle a single common entry point for all import types.
    - Consolidate common import logic in `src/ui/pages/vpn_page.rs`. If there are duplicate logic for file import vs clipboard import, merge them into a single `handle_import(url: String)` function.
    - Prepare for future QR-code support by making the import function accept a generic string input.
    - Reference: User instruction 7.
  </action>
  <verify>
    <automated>grep -r "parse_vpn_key" src/ui/pages/vpn_page.rs | wc -l</automated>
  </verify>
  <done>Import logic is merged and prepared for new input methods.</done>
</task>

</tasks>

<verification>
Ensure Xray files are gone and app still compiles with Sing-box only logic.
</verification>

<success_criteria>
1. `src/domain/xray_config.rs` is deleted.
2. Binary name "xray" is no longer hardcoded or used as a fallback in the daemon.
3. Import logic is streamlined.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stability-cleanup/06-02-SUMMARY.md`
</output>
