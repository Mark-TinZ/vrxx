---
phase: 06-stability-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/pages/vpn_page.rs, src/ui/pages/vpn_page.ui]
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Application starts with an empty key list when no keys are saved"
    - "Header bar subtitle shows 'Ready' or similar instead of dummy connection info on startup"
    - "Disconnect button is disabled when no keys are present"
  artifacts:
    - path: "src/ui/pages/vpn_page.rs"
      provides: "Updated setup_model without dummy keys"
    - path: "src/ui/pages/vpn_page.ui"
      provides: "Neutral default subtitle"
---

<objective>
Remove hardcoded dummy keys and ensure the UI starts in a clean, neutral state when no configuration exists.
</objective>

<execution_context>
@/home/mihail/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-stability-cleanup/06-stability-cleanup-CONTEXT.md
@src/ui/pages/vpn_page.rs
@src/ui/pages/vpn_page.ui
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dummy keys from setup_model</name>
  <files>src/ui/pages/vpn_page.rs</files>
  <action>
    - Open src/ui/pages/vpn_page.rs.
    - Locate the setup_model function.
    - Remove the block that populates the model with dummy keys (key1, key2, key3) when saved_keys.is_empty().
    - Ensure the model remains empty but still replaces the old one in self.imp().model.
  </action>
  <verify>
    <automated>grep -c "Mark-Vless" src/ui/pages/vpn_page.rs</automated>
  </verify>
  <done>Dummy key generation code is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update default UI state in XML</name>
  <files>src/ui/pages/vpn_page.ui</files>
  <action>
    - Open src/ui/pages/vpn_page.ui.
    - Locate the AdwWindowTitle with id="window_title".
    - Change the "subtitle" property from "Mark-Vless Connected" to a translatable "Ready" or "Disconnected".
    - (Optional) Add a placeholder widget to the GtkListBox if desired for better UX when empty.
  </action>
  <verify>
    <automated>grep "Ready" src/ui/pages/vpn_page.ui</automated>
  </verify>
  <done>UI template shows neutral state by default.</done>
</task>

</tasks>

<success_criteria>
- App launches with 0 keys if config is fresh.
- No 'Mark-Vless' or other dummy data appears.
- Disconnect button is disabled on fresh start.
</success_criteria>
