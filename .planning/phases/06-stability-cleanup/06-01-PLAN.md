---
phase: 06-stability-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, meson.build, src/ui/pages/vpn_page.ui, src/ui/pages/vpn_page.rs, src/ui/pages/settings_page.ui, src/ui/pages/settings_page.rs]
autonomous: true
requirements: [CORE-01, UI-01, UI-02]
---

<objective>
Update project version to 0.1.8, fix Russian tooltips, and remove development dummy keys.

Purpose: Prepare the application for production use by removing test data and refining the UI.
Output: Updated project metadata and a cleaner initial state for the VPN page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@Cargo.toml
@meson.build
@src/ui/pages/vpn_page.rs
@src/ui/pages/vpn_page.ui
@src/ui/pages/settings_page.rs
@src/ui/pages/settings_page.ui
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version Update</name>
  <files>Cargo.toml, meson.build</files>
  <action>
    - Update the version in Cargo.toml from 0.1.7 to 0.1.8.
    - Update the version in meson.build from 0.1.7 to 0.1.8.
    - Reference: User instruction 1.
  </action>
  <verify>
    <automated>grep 'version = "0.1.8"' Cargo.toml && grep "version: '0.1.8'" meson.build</automated>
  </verify>
  <done>Project version is bumped to 0.1.8 in both build systems.</done>
</task>

<task type="auto">
  <name>Task 2: UI Localization & Cleanup</name>
  <files>src/ui/pages/vpn_page.ui, src/ui/pages/vpn_page.rs</files>
  <action>
    - Fix missing Russian tooltips in `src/ui/pages/vpn_page.ui` for import buttons (ensure `translatable="yes"` and add Russian translations to `.po` files if needed, though usually, we just ensure the XML has the property).
    - Actually, ensure the `tooltip-text` property is present and marked as translatable for all buttons in the header of the VPN page.
    - Remove dummy key generation logic in `src/ui/pages/vpn_page.rs` within the `setup_model` method (lines 104-113 approx). The model should remain empty if no keys are found.
    - Per D-01: Remove test data (dummy keys) from the VPN page.
  </action>
  <verify>
    <automated>grep -A 5 "import_clipboard_btn" src/ui/pages/vpn_page.ui | grep "tooltip-text" && ! grep "Mark-Vless" src/ui/pages/vpn_page.rs</automated>
  </verify>
  <done>Russian tooltips are fixed and dummy keys are removed from the codebase.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Xray Core Selection</name>
  <files>src/ui/pages/settings_page.ui, src/ui/pages/settings_page.rs</files>
  <action>
    - Remove the "Core Selection" row from `src/ui/pages/settings_page.ui` (id="core_selection_row").
    - Clean up `src/ui/pages/settings_page.rs` to remove the `TemplateChild` for `core_selection_row` and its signal handlers.
    - This prepares for Phase 06-02 where Xray logic will be completely removed.
  </action>
  <verify>
    <automated>! grep "core_selection_row" src/ui/pages/settings_page.ui && ! grep "core_selection_row" src/ui/pages/settings_page.rs</automated>
  </verify>
  <done>Xray core selection is removed from the UI.</done>
</task>

</tasks>

<verification>
Check version bump, verify UI file structure, and ensure dummy keys are gone.
</verification>

<success_criteria>
1. App version is 0.1.8.
2. VPN page starts with an empty list if no keys are saved.
3. Settings page no longer shows core selection.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stability-cleanup/06-01-SUMMARY.md`
</output>
