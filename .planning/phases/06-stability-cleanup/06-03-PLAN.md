---
phase: 06-stability-cleanup
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [src/domain/singbox_config.rs, src/main.rs, src/daemon/mod.rs, src/ui/pages/vpn_page.rs]
autonomous: true
requirements: [CORE-01, NET-01]
---

<objective>
Enhance Sing-box stability and implement log rotation to ensure reliable operation and debuggability.

Purpose: Fix critical networking bugs and prevent log files from growing indefinitely.
Output: Stable Sing-box TUN mode and a reliable logging system with rotation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/domain/singbox_config.rs
@src/main.rs
@src/daemon/mod.rs
@src/ui/pages/vpn_page.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sing-box Stability Fixes</name>
  <files>src/domain/singbox_config.rs</files>
  <action>
    - Debug and fix Sing-box proxy and TUN mode.
    - Specifically, in `src/domain/singbox_config.rs`, ensure that the configuration correctly supports Sing-box 1.11+ changes (sniffing moved from inbounds to rules).
    - If TUN mode is active, make sure the config is optimal for GNOME environment (e.g., proper routing exclusion for LAN).
    - If Sing-box 1.12+ is detected, use the new `domain_resolver` pattern for outbounds.
    - Reference: User instruction 5.
  </action>
  <verify>
    <automated>cargo test domain::singbox_config</automated>
  </verify>
  <done>Sing-box configuration is hardened for version-specific behaviors and TUN mode is stable.</done>
</task>

<task type="auto">
  <name>Task 2: Logging UI & Console Fix</name>
  <files>src/ui/pages/vpn_page.rs, src/main.rs</files>
  <action>
    - Fix the UI log console in `src/ui/pages/vpn_page.rs`. Ensure that it correctly receives log lines from the daemon via D-Bus and appends them to the console view.
    - If there are performance issues with log display (scrolling/memory), implement a limit on the number of lines kept in the UI console.
    - Reference: User instruction 6.
  </action>
  <verify>
    <automated>grep "log_console" src/ui/pages/vpn_page.rs && grep "Log lines" src/ui/pages/vpn_page.rs</automated>
  </verify>
  <done>UI log console is functional and performant.</done>
</task>

<task type="auto">
  <name>Task 3: Log Rotation & Archiving</name>
  <files>src/main.rs</files>
  <action>
    - Implement log rotation for `core.log` using the `tracing-appender` crate or a custom solution in `src/main.rs`.
    - Configure it to keep a certain number of daily archives and rotate after the file reaches a specific size (e.g., 10MB).
    - Reference: User instruction 6.
  </action>
  <verify>
    <automated>grep "tracing_appender" Cargo.toml && grep "RollingFileAppender" src/main.rs</automated>
  </verify>
  <done>Log rotation is implemented for the core log file.</done>
</task>

</tasks>

<verification>
Run all tests, verify logging console in UI, and check for rotated log files.
</verification>

<success_criteria>
1. Sing-box proxy starts and routes traffic correctly in both proxy and TUN modes.
2. UI logs show real-time output from the proxy engine.
3. `core.log` is automatically rotated and archived.
</success_criteria>

<output>
After completion, create `.planning/phases/06-stability-cleanup/06-03-SUMMARY.md`
</output>
